<%
/* 相対パスを取得 */
var setLayered = ("../").repeat(filename.slice(filename.indexOf("_src")).split("/").length - 1);

/* ページID */
var pageId = "sample";
%>
<!DOCTYPE html>
<html lang="ja">
<%- include(setLayered + "template/head", { pageId: pageId }) %>
<body>

<%- include(setLayered + "template/header", { pageId: pageId }) %>

</body>
</html>